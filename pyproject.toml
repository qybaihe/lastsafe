[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "lastsafe"
version = "0.1.0"
description = "An autonomous Alpaca paper-trading agent for options expiry management"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "LastSafe Team" }]
dependencies = [
  "fastapi>=0.116.0,<1.0",
  "httpx>=0.28.0,<1.0",
  "uvicorn[standard]>=0.35.0,<1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4.0,<9.0",
  "pytest-asyncio>=1.1.0,<2.0",
  "ruff>=0.12.0,<1.0",
]

[project.scripts]
lastsafe = "lastsafe.main:run"

[tool.hatch.build.targets.wheel]
packages = ["src/lastsafe"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
